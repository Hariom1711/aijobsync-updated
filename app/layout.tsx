// app/layout.tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "sonner";
import Providers from "./providers";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "AIJobSync — ATS-Optimized Resumes in Seconds",
  description:
    "Build tailored, ATS-optimized resumes powered by AI. Upload a job description, match your profile, and generate a standout resume instantly.",
  keywords: ["resume builder", "ATS resume", "AI resume", "job application", "career"],
  openGraph: {
    title: "AIJobSync",
    description: "AI-powered resume builder that beats ATS filters",
    type: "website",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <Providers>
          {children}
          <Toaster richColors position="top-right" />
        </Providers>
      </body>
    </html>
  );
}
